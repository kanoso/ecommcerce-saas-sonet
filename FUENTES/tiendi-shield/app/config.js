/**
 * Tiendi Shield — configuración de entorno.
 * E2.3: destino de la API del registro.
 * TODO(C02 producción): apuntar a https://api.tiendi.pe/api/v1 cuando se
 * apruebe la publicación. No inventar URLs de producción.
 */
window.SHIELD_CONFIG = {
  API_BASE: 'http://localhost:4000/api/v1',
};
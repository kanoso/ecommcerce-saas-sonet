{
  "name": "@kanoso/auth-types",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos compartidos de autenticación Tiendi (Role, StoreRole, User, AuthSession, ApiAuthResponse). Solo tipos, cero runtime (A2).",
  "types": "./src/index.ts",
  "scripts": {
    "test:types": "tsc --noEmit -p tsconfig.test.json"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
